apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# namespace: ghost  # Don't set - namespaces are defined explicitly in namespace.yaml

resources:
  - namespace.yaml
  - configmaps.yaml
  - storage.yaml
  - network-policies.yaml

# Secrets are managed separately via External Secrets or Sealed Secrets
# - secrets.yaml (template only, not applied directly)

commonLabels:
  app.kubernetes.io/part-of: project-ghost
  app.kubernetes.io/managed-by: flux
