apiVersion: v1
kind: Secret
metadata:
  name: api-auth
  namespace: ghost
  labels:
    app: openresty
type: Opaque
# htpasswd value will be set via: kubectl create secret generic api-auth --from-literal=htpasswd='ghost:$apr1$...'
# Or populated from GitHub secrets in CI/CD
