# Flux GitOps Toolkit Components
# This is a placeholder - actual components are installed via:
# flux install --export > gotk-components.yaml
#
# For installation, run:
# flux bootstrap github \
#   --owner=YOUR_GITHUB_USER \
#   --repository=project-ghost \
#   --branch=master \
#   --path=k8s/flux-system \
#   --personal
#
# Or for organization repositories:
# flux bootstrap github \
#   --owner=YOUR_ORG \
#   --repository=project-ghost \
#   --branch=master \
#   --path=k8s/flux-system \
#   --team=YOUR_TEAM

---
# Note: The actual gotk-components.yaml will be generated by flux bootstrap
# This file serves as documentation for the expected installation method
